#include "Logger.h"
#include "vm/GlobalMetadataFileInternals.h"

void MDebugLog::LogMetadataHeader(const Il2CppGlobalMetadataHeader *header) {
    auto &logger = MLogger;
    logger.debug("sanity: {}", header->sanity);
    logger.debug("version: {}", header->version);
    logger.debug("stringLiteralOffset: {}", header->stringLiteralOffset);
    logger.debug("stringLiteralSize: {}", header->stringLiteralSize);
    logger.debug("stringLiteralDataOffset: {}", header->stringLiteralDataOffset);
    logger.debug("stringLiteralDataSize: {}", header->stringLiteralDataSize);
    logger.debug("stringOffset: {}", header->stringOffset);
    logger.debug("stringSize: {}", header->stringSize);
    logger.debug("eventsOffset: {}", header->eventsOffset);
    logger.debug("eventsSize: {}", header->eventsSize);
    logger.debug("propertiesOffset: {}", header->propertiesOffset);
    logger.debug("propertiesSize: {}", header->propertiesSize);
    logger.debug("methodsOffset: {}", header->methodsOffset);
    logger.debug("methodsSize: {}", header->methodsSize);
    logger.debug("parameterDefaultValuesOffset: {}", header->parameterDefaultValuesOffset);
    logger.debug("parameterDefaultValuesSize: {}", header->parameterDefaultValuesSize);
    logger.debug("fieldDefaultValuesOffset: {}", header->fieldDefaultValuesOffset);
    logger.debug("fieldDefaultValuesSize: {}", header->fieldDefaultValuesSize);
    logger.debug("fieldAndParameterDefaultValueDataOffset: {}", header->fieldAndParameterDefaultValueDataOffset);
    logger.debug("fieldAndParameterDefaultValueDataSize: {}", header->fieldAndParameterDefaultValueDataSize);
    logger.debug("fieldMarshaledSizesOffset: {}", header->fieldMarshaledSizesOffset);
    logger.debug("fieldMarshaledSizesSize: {}", header->fieldMarshaledSizesSize);
    logger.debug("parametersOffset: {}", header->parametersOffset);
    logger.debug("parametersSize: {}", header->parametersSize);
    logger.debug("fieldsOffset: {}", header->fieldsOffset);
    logger.debug("fieldsSize: {}", header->fieldsSize);
    logger.debug("genericParametersOffset: {}", header->genericParametersOffset);
    logger.debug("genericParametersSize: {}", header->genericParametersSize);
    logger.debug("genericParameterConstraintsOffset: {}", header->genericParameterConstraintsOffset);
    logger.debug("genericParameterConstraintsSize: {}", header->genericParameterConstraintsSize);
    logger.debug("genericContainersOffset: {}", header->genericContainersOffset);
    logger.debug("genericContainersSize: {}", header->genericContainersSize);
    logger.debug("nestedTypesOffset: {}", header->nestedTypesOffset);
    logger.debug("nestedTypesSize: {}", header->nestedTypesSize);
    logger.debug("interfacesOffset: {}", header->interfacesOffset);
    logger.debug("interfacesSize: {}", header->interfacesSize);
    logger.debug("vtableMethodsOffset: {}", header->vtableMethodsOffset);
    logger.debug("vtableMethodsSize: {}", header->vtableMethodsSize);
    logger.debug("interfaceOffsetsOffset: {}", header->interfaceOffsetsOffset);
    logger.debug("interfaceOffsetsSize: {}", header->interfaceOffsetsSize);
    logger.debug("typeDefinitionsOffset: {}", header->typeDefinitionsOffset);
    logger.debug("typeDefinitionsSize: {}", header->typeDefinitionsSize);
    logger.debug("imagesOffset: {}", header->imagesOffset);
    logger.debug("imagesSize: {}", header->imagesSize);
    logger.debug("assembliesOffset: {}", header->assembliesOffset);
    logger.debug("assembliesSize: {}", header->assembliesSize);
    logger.debug("fieldRefsOffset: {}", header->fieldRefsOffset);
    logger.debug("fieldRefsSize: {}", header->fieldRefsSize);
    logger.debug("referencedAssembliesOffset: {}", header->referencedAssembliesOffset);
    logger.debug("referencedAssembliesSize: {}", header->referencedAssembliesSize);
    logger.debug("attributeDataOffset: {}", header->attributeDataOffset);
    logger.debug("attributeDataSize: {}", header->attributeDataSize);
    logger.debug("attributeDataRangeOffset: {}", header->attributeDataRangeOffset);
    logger.debug("attributeDataRangeSize: {}", header->attributeDataRangeSize);
    logger.debug("unresolvedIndirectCallParameterTypesOffset: {}", header->unresolvedIndirectCallParameterTypesOffset);
    logger.debug("unresolvedIndirectCallParameterTypesSize: {}", header->unresolvedIndirectCallParameterTypesSize);
    logger.debug("unresolvedIndirectCallParameterRangesOffset: {}", header->unresolvedIndirectCallParameterRangesOffset);
    logger.debug("unresolvedIndirectCallParameterRangesSize: {}", header->unresolvedIndirectCallParameterRangesSize);
    logger.debug("windowsRuntimeTypeNamesOffset: {}", header->windowsRuntimeTypeNamesOffset);
    logger.debug("windowsRuntimeTypeNamesSize: {}", header->windowsRuntimeTypeNamesSize);
    logger.debug("windowsRuntimeStringsOffset: {}", header->windowsRuntimeStringsOffset);
    logger.debug("windowsRuntimeStringsSize: {}", header->windowsRuntimeStringsSize);
    logger.debug("exportedTypeDefinitionsOffset: {}", header->exportedTypeDefinitionsOffset);
    logger.debug("exportedTypeDefinitionsSize: {}", header->exportedTypeDefinitionsSize);
}